[project]
name = "photon-docker"
version = "1.0.0"
description = "Unofficial docker image for the Photon Geocoder"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-crontab==3.2.0",
    "requests==2.32.4",
    "supervisor>=4.2.5",
    "tqdm==4.67.1",
]
