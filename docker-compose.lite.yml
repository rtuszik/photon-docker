services:
    photon:
        image: rtuszik/photon-docker:lite
        # environment:
        # - COUNTRY_CODE=zw
        volumes:
            - photon_data:/photon/photon_data
        restart: unless-stopped
        ports:
            - "2322:2322"
volumes:
    photon_data:
